using System;
using System.Collections;
using System.IO;

namespace NameSorter
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.ForegroundColor = ConsoleColor.Green;
            //FileService fileService = new FileService("unsorted-names-list.txt");
            //Boolean flag = fileService.ReadFromFile();
            //Console.Write(flag);
            //fileService.WriteToFile();
            Console.ResetColor();
        }
    }
}
